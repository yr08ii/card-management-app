/** S14 — Change password (design spec §3.1, PRD F8/AC2). */

import { RouteStub } from '@/components/route-stub';

export default function ChangePasswordScreen() {
  return <RouteStub name="S14 — Change password" />;
}
